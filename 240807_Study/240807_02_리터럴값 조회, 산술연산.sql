select 22;
select 3.14;
select "안녕";
select '홑따옴표도 문자열';

select true;  -- true 값 1
select false;  -- false 값 0

select 23+45; -- 산술연산도 가능
select 2*4;
select 10/3;  -- 실수연산도 해줌
select 10%7; -- 나머지 연산도 가능

select "문자열" + "결합"; -- 문자열 합연산은 안됨, 결합 불가
select "10ㅋㅋ" + "20ㅁㄴㅇㄹ"; 
-- 문자열이 있더라도 +가 있으면 숫자로 바꿔서 계산을 해보고 출력함